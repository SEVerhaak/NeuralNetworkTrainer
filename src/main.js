import './style.css'
import './faceTracking.js'

/*
ml5.setBackend("webgl");
const neuralNetwork = ml5.neuralNetwork({ task: 'classification', debug: true })

neuralNetwork.addData([18,9.2,8.1,2], {label:"cat"})
neuralNetwork.addData([20.1,17,15.5,5], {label:"dog"})

neuralNetwork.normalizeData()
neuralNetwork.train({ epochs: 10 }, () => finishedTraining())
async function finishedTraining(){
    const results = await neuralNetwork.classify([29,11,10,3])
    console.log(results)
}

 */